#!/bin/sh
if [ ! -d /var/log/ceph/runit ]; then mkdir /var/log/ceph/runit; fi
if [ ! -d /var/log/ceph/runit/osd.<%= @options['osd_id'] %> ]; then mkdir /var/log/ceph/runit/osd.<%= @options['osd_id'] %>; fi
exec svlogd -tt /var/log/ceph/runit/osd.<%= @options['osd_id'] %>
